import 'package:flutter/material.dart';

class PatientAppoinmentScreen extends StatelessWidget {
  const PatientAppoinmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Text("Appoinment")
        ],
      ),
    );
  }
}
